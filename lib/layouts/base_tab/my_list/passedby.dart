import 'package:flutter/material.dart';

class Passedby extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('صفحة زار ملفي'),
    );
  }
}